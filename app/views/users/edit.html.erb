<% provide(:title, "Edit User") %>
<% provide(:name, "Editing User") %>

<%= render 'form', user: @user %>

<%= button_to 'Show', @user, method: :get, class: 'btn btn-primary' %>
<%= button_to 'Back', users_path, method: :get, class: 'btn btn-primary' %>
