<% provide(:title, "Edit Project") %>
<% provide(:name, "Editing Project") %>

<%= render 'form', project: @project %>

<div class="bttn">
<%= button_to 'Back', request.referrer, method: :get, class: 'btn btn-primary' %>
</div>